language: cpp
script:
        - git submodule update --init
        - mkdir build && cd build
        - cmake ..
        - make test
addons:
        apt:
                sources:
                        - george-edison55-precise-backports
                packages:
                        - cmake-data
                        - cmake
